package com.ciq.exceptiondemo;

public class NeedOffiles {

	public static void main(String[] args) {
		String messages = "What is a Checked Exception in Java ? "
				+ "Checked exceptions are the ones that are checked during "
				+ "compilation time. If a code within a method throws a checked"
				+ " exception, then it should either be handled by a method or"
				+ " specify it using the throws keyword.";
		System.out.println(messages);
	}
}
